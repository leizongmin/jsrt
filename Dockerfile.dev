# Dockerfile for jsrt development with Claude Code
FROM ubuntu:22.04

# Set environment variables to avoid interactive prompts
ENV DEBIAN_FRONTEND=noninteractive
ENV TZ=UTC

# Set up Chinese mirror for faster package downloads (using HTTP first to avoid certificate issues)
RUN sed -i 's|http://archive.ubuntu.com/ubuntu/|http://mirrors.aliyun.com/ubuntu/|g' /etc/apt/sources.list \
    && sed -i 's|http://security.ubuntu.com/ubuntu/|http://mirrors.aliyun.com/ubuntu/|g' /etc/apt/sources.list

# Install available GCC version from Ubuntu repository (faster, no PPA needed)
RUN apt-get update && apt-get install -y \
    build-essential \
    gcc-12 \
    g++-12 \
    cmake \
    git \
    wget \
    curl \
    vim \
    nano \
    libssl-dev \
    libffi-dev \
    ca-certificates \
    gnupg \
    lsb-release \
    pkg-config \
    python3 \
    python3-pip \
    python-is-python3 \
    fish \
    htop \
    libkrb5-dev \
    libgssapi-krb5-2 \
    && update-alternatives --install /usr/bin/gcc gcc /usr/bin/gcc-12 100 \
    && update-alternatives --install /usr/bin/g++ g++ /usr/bin/g++-12 100
# RUN rm -rf /var/lib/apt/lists/*

# Install clang-format-20 from LLVM APT repository (using Tsinghua mirror)
RUN wget -qO- https://apt.llvm.org/llvm-snapshot.gpg.key | apt-key add - \
    && echo "deb https://mirrors.tuna.tsinghua.edu.cn/llvm-apt/jammy/ llvm-toolchain-jammy-20 main" > /etc/apt/sources.list.d/llvm.list \
    && apt-get update \
    && apt-get install -y clang-format-20 \
    && ln -sf /usr/bin/clang-format-20 /usr/bin/clang-format \
    && apt-get clean \
    && rm -rf /var/lib/apt/lists/* \
    && echo "Installed clang-format version:" \
    && clang-format --version

# Install Node.js 22 from NodeSource (stable and reliable)
RUN curl -fsSL https://deb.nodesource.com/setup_22.x | bash - \
    && apt-get install -y nodejs \
    && npm config set registry https://registry.npmmirror.com \
    && rm -rf /var/lib/apt/lists/*

# Install Code Server
RUN export VERSION=4.104.3 \
    && cd /tmp \
    && curl -fOL https://github.com/coder/code-server/releases/download/v$VERSION/code-server_${VERSION}_amd64.deb \
    && dpkg -i code-server_${VERSION}_amd64.deb \
    && rm -f code-server_${VERSION}_amd64.deb

# Install Claude Code and Codex
RUN npm i -g @anthropic-ai/claude-code@^2.0.19 @openai/codex@0.46.0

# Set up working directory
WORKDIR /repo

# Create entrypoint script
RUN echo '#!/bin/bash' > /usr/local/bin/entrypoint \
    && echo 'cd /repo' >> /usr/local/bin/entrypoint \
    && echo '# Fix git ownership issue in Docker for repo and submodules' >> /usr/local/bin/entrypoint \
    && echo 'git config --global --add safe.directory "*" 2>/dev/null || true' >> /usr/local/bin/entrypoint \
    && echo '# Configure git user from environment variables' >> /usr/local/bin/entrypoint \
    && echo 'if [ -n "$GIT_USER_NAME" ]; then' >> /usr/local/bin/entrypoint \
    && echo '  git config --global user.name "$GIT_USER_NAME"' >> /usr/local/bin/entrypoint \
    && echo '  git config user.name "$GIT_USER_NAME"' >> /usr/local/bin/entrypoint \
    && echo 'fi' >> /usr/local/bin/entrypoint \
    && echo 'if [ -n "$GIT_USER_EMAIL" ]; then' >> /usr/local/bin/entrypoint \
    && echo '  git config --global user.email "$GIT_USER_EMAIL"' >> /usr/local/bin/entrypoint \
    && echo '  git config user.email "$GIT_USER_EMAIL"' >> /usr/local/bin/entrypoint \
    && echo 'fi' >> /usr/local/bin/entrypoint \
    && echo 'export BASH_DEFAULT_TIMEOUT_MS=600000' >> /usr/local/bin/entrypoint \
    && echo 'export BASH_MAX_TIMEOUT_MS=600000' >> /usr/local/bin/entrypoint \
    && echo 'exec "$@"' >> /usr/local/bin/entrypoint \
    && chmod +x /usr/local/bin/entrypoint

# Set the entrypoint
ENTRYPOINT ["/usr/local/bin/entrypoint"]
CMD []
